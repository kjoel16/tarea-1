using System.Text.RegularExpressions;

public static string LimpiarBasico(string? texto)
{
    if (string.IsNullOrWhiteSpace(texto))
        return string.Empty;

    // Quita todo lo que no sea A-Z, a-z, 0-9 o espacio
    return Regex.Replace(texto, @"[^a-zA-Z0-9 ]", "");
}