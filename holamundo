// Supongamos que tu objeto principal se llama 'response'
if (response.Totals != null && response.Totals.Length > 1)
{
    response.Totals = response.Totals
        .OrderBy(t => t.Total.Currency == "HNL" ? 0 : 1)
        .ThenBy(t => t.Total.Currency == "USD" ? 1 : 2)
        .ToArray();
}