if (!program.run()) {
    AS400Message[] msgs = program.getMessageList();

    for (AS400Message m : msgs) {
        System.out.println("AS400 MSG: " + m.getText());
    }

    throw new Exception("Error llamando programa AS400");
}