package com.mycompany.api.transaccions.dto;

public class ResponseDTO {

    private HeaderResponseDTO header;
    private Object data;

    public HeaderResponseDTO getHeader() { return header; }
    public void setHeader(HeaderResponseDTO header) { this.header = header; }

    public Object getData() { return data; }
    public void setData(Object data) { this.data = data; }
}