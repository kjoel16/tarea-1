String user = AES256Decrypt.decrypt(props.getUser(), props.getKey());
String pass = AES256Decrypt.decrypt(props.getPassword(), props.getKey());

AS400 as400 = new AS400(
    props.getHost(),
    user,
    pass
);