@RestController
@RequestMapping("/cuenta")
public class ConsultaCuentaController {

    @Autowired
    private ConsultaCuentaService service;

    @PostMapping("/consultar")
    public ConsultaCuentaDataDTO consultar(@RequestBody ConsultaCuentaRequestDTO request) {
        return service.consultarDatosCliente(request);
    }
}