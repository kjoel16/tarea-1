CREATE OR REPLACE PROCEDURE TDIAZQDD.SP_CONSULTAR_CUENTA (
    IN P_CUENTA CHAR(20)
)
LANGUAGE SQL
BEGIN

    SELECT TNOMBRE,
           TSALDO,
           TESTADO,
           TTIPO
    FROM TDIAZQDD.TAP002
    WHERE TRIM(TACCNT) = TRIM(P_CUENTA);

END